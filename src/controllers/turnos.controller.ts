
export const getAll = ((_req: Request, res: any) => {
    res.send('Controller from turnos');
    return this;
})

export const getById = ((_req: Request, res: any) => {
    res.send('Controller from turnos');
    return;
})

export const store = ((_req: Request, res: any) => {
    res.send('Controller from turnos');
    return;
})

export const update = ((_req: Request, res: any) => {
    res.send('Controller from turnos');
    return;
})

export const remove = ((_req: Request, res: any) => {
    res.send('Controller from turnos');
    return;
})